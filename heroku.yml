build:
  docker:
    web: Dockerfile
# run:
#   # web: newrelic-admin run-program uvicorn ghs.view.web.endpoints:app --workers 4 --host 0.0.0.0 --port $PORT --loop uvloop --http httptools --interface asgi3 --log-level info
#   web: newrelic-admin run-program uvicorn ghs.view.web.endpoints:app --host 0.0.0.0 --port $PORT --loop uvloop --http httptools --interface asgi3 --log-level info
